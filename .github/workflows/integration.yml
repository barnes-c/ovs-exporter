---
name: Integration tests
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  integration:
    name: docker-compose smoke
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false
      - run: echo "Integration tests stub — implemented in M1 task #14 (docker-compose with openvswitch/ovs container, expfmt scrape, ovs-vsctl mutations)."
